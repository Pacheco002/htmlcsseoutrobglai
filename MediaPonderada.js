function mediaponderada(){Array.
    var total = 0;
    for (var i = 0; i <)
    total += media [i] *
var avg = total /
return avg:

const numbers = [1, 2, 3, 4, 5];
const weights = [1, 2, 3, 4, 5];
const avg =
console.log(avg); //output:
}